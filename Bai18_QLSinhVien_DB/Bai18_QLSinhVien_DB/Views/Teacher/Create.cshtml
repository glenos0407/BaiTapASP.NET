@model Entity.Teacher_Ent

@{
    ViewBag.Title = "Create Teacher";
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" type="text/css" />
    <link rel="stylesheet" href="~/Content/all.css" type="text/css">
    <script src="~/Scripts/modernizr-2.8.3.js"></script>
    <script type="text/javascript">
        if (("@ViewBag.Message").trim() != "") {
            alert("@ViewBag.Message");
        }
    </script>
    <style type="text/css">
        body {
            background-color: dimgray;
        }

        #frmCreate {
            text-align: center;
            width: 30vw;
            background-color: rgba(45,45,45, 0.6);
            padding: 2em;
        }

        #box-center {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 80vh;
        }

        label {
            color: white;
        }

        #txtID, #txtName {
            border-radius: 0px;
            color: white;
            background-color: transparent;
            outline: none;
            border-top: none;
            border-left: none;
            border-right: none;
            border-bottom: 1px solid #646464;
        }

        #btnSave {
            border-radius: 0;
            padding: 0.5em 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row" style="padding: 2em 0;">
            <a href="@Url.Action("Teacher","Teacher")">< Back To Home</a>
        </div>
        <div id="box-center">
            @using (Html.BeginForm("Create", "Teacher", FormMethod.Post, new { @id = "frmCreate" }))
            {
                <div class="form-group" style="text-align: left;">
                    <label for="txtID"><b>Teacher ID</b></label>
                    @Html.TextBoxFor(model => model.TeacherID, new { @class = "form-control", @id = "txtID" })
                    @Html.ValidationMessageFor(model => model.TeacherID, "", new { @style = "color: red;" })
                </div>

                <div class="form-group" style="text-align: left;">
                    <label for="txtName"><b>Teacher Name</b></label>
                    @Html.TextBoxFor(model => model.TeacherName, new { @class = "form-control", @id = "txtName", required = "required" })
                    @Html.ValidationMessageFor(model => model.TeacherName, "", new { @style = "color: red;" })
                </div>

                <br />

                <input type="submit" class="btn btn-success" value="Save" id="btnSave" style="width: 10vw;" />
            }
        </div>
    </div>
    <script src="~/Scripts/jquery-3.4.1.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
</body>
</html>


